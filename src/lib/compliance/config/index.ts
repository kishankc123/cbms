import type { CountryConfig } from "./types";
import { NEPAL } from "./nepal";

/** Countries shipped with the app. Adding one is adding a CountryConfig here (or, later, rows in the database). */
export const COUNTRY_CONFIGS: CountryConfig[] = [NEPAL];
